<%= form_for(utf_item) do |f| %>
  <% if utf_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(utf_item.errors.count, "error") %> prohibited this utf_item from being saved:</h2>

      <ul>
      <% utf_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
